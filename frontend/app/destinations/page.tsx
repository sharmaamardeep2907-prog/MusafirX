'use client';
import { useState } from 'react';
import Link from 'next/link';
import { motion } from 'framer-motion';
import { useQuery } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { cn } from '@/lib/utils';
import { Search, MapPin, Star, TrendingUp, Loader2, Mountain, Waves, Castle, Church, Leaf, Camera, Route } from 'lucide-react';

const typeIcons:Record<string,any>={'Heritage':Castle,'Beach':Waves,'Hill Station':Mountain,'Spiritual':Church,'Nature':Leaf,'Adventure':Route,'Desert':Route,'Urban':Camera,'Backwaters':Waves};
const typeColors:Record<string,string>={'Heritage':'bg-amber-50 text-amber-700','Beach':'bg-cyan-50 text-cyan-700','Hill Station':'bg-blue-50 text-blue-700','Spiritual':'bg-purple-50 text-purple-700','Nature':'bg-emerald-50 text-emerald-700','Adventure':'bg-red-50 text-red-700','Desert':'bg-orange-50 text-orange-700','Urban':'bg-gray-50 text-gray-700','Backwaters':'bg-teal-50 text-teal-700'};

export default function DestinationsPage() {
  const [search,setSearch]=useState('');
  const [typeFilter,setTypeFilter]=useState('');
  const {data,isLoading}=useQuery({queryKey:['destinations',search],queryFn:()=>api.get<any>('/destinations',{search,limit:'50'})});
  const types=['Heritage','Beach','Hill Station','Spiritual','Nature','Adventure','Desert'];
  const filtered=data?.destinations?.filter((d:any)=>!typeFilter||d.type===typeFilter)||[];

  return (<div className="pt-24 pb-16 bg-ivory min-h-screen"><div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div className="text-center mb-12"><h1 className="text-4xl md:text-5xl font-display font-bold text-navy mb-4">Discover India</h1><p className="text-lg text-charcoal/60 max-w-2xl mx-auto">Explore hundreds of destinations across India — each with its own story.</p></div><div className="flex flex-col sm:flex-row gap-4 mb-10"><div className="flex-1 relative"><Search className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-charcoal/40"/><input type="text" placeholder="Search destinations..." value={search} onChange={e=>setSearch(e.target.value)} className="input-field pl-12"/></div><div className="flex gap-2 flex-wrap"><button onClick={()=>setTypeFilter('')} className={cn('px-4 py-2 rounded-xl text-sm font-medium transition-colors',!typeFilter?'bg-navy text-white':'bg-white text-charcoal border border-softgray hover:bg-saffron/5')}>All</button>{types.map(t=><button key={t} onClick={()=>setTypeFilter(typeFilter===t?'':t)} className={cn('px-4 py-2 rounded-xl text-sm font-medium transition-colors flex items-center gap-1.5',typeFilter===t?'bg-navy text-white':'bg-white text-charcoal border border-softgray hover:bg-saffron/5')}>{(()=>{const Icon=typeIcons[t]||MapPin;return <Icon className="w-4 h-4"/>;})()}{t}</button>)}</div></div>{isLoading&&<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{Array.from({length:9}).map((_,i)=><div key={i} className="rounded-2xl bg-white h-80 skeleton"/>)}</div>}{!isLoading&&<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{filtered.map((dest:any,i:number)=>(<motion.div key={dest.id} initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} transition={{delay:i*0.05}}><Link href={`/destinations/${dest.slug}`}><div className="neumorphic-card overflow-hidden group h-full"><div className="relative h-48 overflow-hidden"><div className="absolute inset-0 bg-cover bg-center group-hover:scale-105 transition-transform duration-700" style={{backgroundImage:`url(${dest.imageUrl||'https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=600'})`}}/><div className="absolute inset-0 bg-gradient-to-t from-black/50 to-transparent"/><div className="absolute top-3 left-3 flex gap-2"><span className={cn('px-2.5 py-1 rounded-full text-xs font-medium',typeColors[dest.type]||'bg-gray-50 text-gray-700')}>{dest.type}</span>{dest.isTrending&&<span className="px-2.5 py-1 rounded-full text-xs font-medium bg-saffron/10 text-saffron flex items-center gap-1"><TrendingUp className="w-3 h-3"/>Trending</span>}</div><div className="absolute bottom-3 left-3 right-3"><div className="flex items-center gap-1 text-white/90 text-sm"><MapPin className="w-3.5 h-3.5"/>{dest.stateId?.name||dest.state?.name}</div></div></div><div className="p-5"><div className="flex items-start justify-between mb-2"><h3 className="text-lg font-bold text-navy group-hover:text-saffron transition-colors">{dest.name}</h3><span className="flex items-center gap-1 text-sm font-semibold text-saffron"><Star className="w-3.5 h-3.5 fill-saffron"/>{dest.rating}</span></div><p className="text-sm text-charcoal/60 line-clamp-2 mb-3">{dest.description}</p><div className="flex items-center justify-between text-xs text-charcoal/50"><span>{dest.budget}</span><span>{dest.bestSeason}</span></div></div></div></Link></motion.div>))}</div>}{!isLoading&&filtered.length===0&&<div className="text-center py-20"><MapPin className="w-16 h-16 text-charcoal/20 mx-auto mb-4"/><h3 className="text-xl font-bold text-navy mb-2">No destinations found</h3><p className="text-charcoal/60">Try adjusting your search or filters.</p></div>}</div></div>);
}
