services:
  mongodb:
    image: mongo:7
    container_name: musafirx-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: musafirx

volumes:
  mongo_data:
